import type { HftTokenDefinition } from "./types"

/**
 * Mainnet HyperFungibleToken registry.
 *
 * Tokens are added here via PR once contracts are deployed and peer chains
 * are registered on-chain. Keep this file empty until mainnet HFT launches.
 */
export const MAINNET_HFT_TOKENS: HftTokenDefinition[] = []
